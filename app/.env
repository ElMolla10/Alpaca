REBALANCE_BAND=0.02
MIN_ABS_POS=0.02
EMA_HL=7
SIGN_MULT=3.0
EOD_FLATTEN_MIN_BEFORE_CLOSE=2
DAY_KILL_DD_PCT = float(os.environ.get("DAY_KILL_DD_PCT", "1.0"))     # stop trading at −1.0%
DAY_THROTTLE_DD_PCT = float(os.environ.get("DAY_THROTTLE_DD_PCT", "0.7"))  # throttle at −0.7%
THROTTLE_SIZE_MULT = float(os.environ.get("THROTTLE_SIZE_MULT", "0.70"))   # keep 70% size after throttle
